server:
  port: 8760

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pandalin/springcloud/tree/master/config-repo
          username:
          password:
      label: master
